import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  main: {
    flexGrow: 1,
    alignItems: 'center',
  },
  keyboardView: {
    flexGrow: 1,
    alignItems: 'center',
  },
});

export default styles;
